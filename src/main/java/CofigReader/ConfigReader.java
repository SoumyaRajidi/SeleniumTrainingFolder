package CofigReader;

/*import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.util.Properties;

public class ConfigReader throws IOException {
        String PropFileLoc="Config/configaration.properties";
        String PropFileLoc2="Config/configaration2.properties";
        void ConfigReader()
        File file=new File(PropFileLoc);// for file location
        File file2=new File(PropFileLoc2);
        FileInputStream fls=new FileInputStream(file);// for reading the file
        FileInputStream fls2=new FileInputStream(file2);
        Properties prop=new Prop{erties(); // Declaring property class
        prop.load(fls); // Loading properties information
        prop.load(fls2);
        String browser= prop.getProperty("browser");
        System.out.println("this is browser value from properties file "+browser);
        String Testurl= prop.getProperty("Testurl");
        System.out.println("this is url value from properties file "+Testurl);
        String username= prop.getProperty("username");
        System.out.println("this is username value from properties file2 "+username);
        String password= prop.getProperty("password");
        System.out.println("this is password value from properties file2 "+password);
    }
} */
